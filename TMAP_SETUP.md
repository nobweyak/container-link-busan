# CONNEXT 차량 위치·TMAP 설정 안내

## 1. 준비 사항

- TMAP에서 발급받은 `appKey`
- TMAP 앱에 **자동차 경로안내 API**와 **JavaScript 지도** 상품이 활성화되어 있어야 합니다.
- 운반자 스마트폰은 GPS를 사용할 수 있어야 합니다.
- 위치 권한은 HTTPS로 배포된 CONNEXT GitHub Pages에서 허용해야 합니다.

## 2. TMAP 키 입력

1. CONNEXT에 컨테이너 필요자 계정으로 로그인합니다.
2. 첫 화면에서 `TMAP 경로 API 연동 설정`을 누릅니다.
3. 발급받은 `appKey`를 입력하고 `키 저장`을 누릅니다.
4. 키는 해당 브라우저의 로컬 저장 공간에만 보관됩니다. GitHub와 Firebase에는 저장되지 않습니다.

## 3. 위치 공유 흐름

1. 컨테이너 필요자가 매칭을 최종 확정합니다.
2. 필요자가 확정 거래의 `차량 위치`를 누르고 `운반자에게 최신 위치 요청`을 누릅니다.
3. 운반자는 같은 확정 거래의 `GPS 공유`를 누릅니다.
4. `현재 운송 위치 공유에 동의합니다`에 체크합니다.
5. 한 번만 전송하려면 `현재 GPS 위치 전송`을 누릅니다.
6. 요청마다 자동 응답하려면 `위치 요청 자동 응답 시작`을 누르고 브라우저 위치 권한을 허용합니다.
7. 필요자는 `위치 정보 새로고침`을 누르면 최신 위치, TMAP 경로, 남은 거리와 예상 시간을 볼 수 있습니다.

## 4. 중요한 제한

- 자동 응답은 운반자의 위치 공유 화면이 열려 있을 때만 작동합니다.
- 화면을 닫으면 위치 요청 감시와 GPS 접근이 즉시 중단됩니다.
- 브라우저가 위치 권한을 거부하면 GPS를 저장할 수 없습니다.
- 현재 로그인은 시연용 익명 인증 구조입니다. 실제 서비스에서 정밀 위치를 사용하기 전에는 Firebase 이메일 로그인과 계정별 보안 규칙으로 전환해야 합니다.
- 시연에서는 위치 공유에 동의한 테스트 기기의 위치만 사용하세요.

## 5. 오류 확인

- `401`: appKey가 잘못되었거나 TMAP 상품이 활성화되지 않은 경우가 많습니다.
- 지도가 나오지 않음: JavaScript 지도 상품과 키를 다시 확인합니다.
- 경로가 나오지 않음: 요청 등록 시 반납지의 위도·경도 또는 부산항 기본 장소 정보가 필요합니다.
- 위치 저장 실패: Firestore 수정 권한과 브라우저 위치 권한을 확인합니다.

TMAP 자동차 경로안내 공식 문서: https://tmap-skopenapi.readme.io/reference/자동차-경로안내
